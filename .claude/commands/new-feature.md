Delegate this feature to the `architect` agent:

> "Implement the '$ARGUMENTS' feature end-to-end. Follow the orchestration steps in your system prompt: explore existing patterns, design a spec, then delegate to the backend, frontend, and test-writer agents."
