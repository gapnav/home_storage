Delegate to the `test-writer` agent:

> "Write tests for: $ARGUMENTS. Read the file first, then write the appropriate spec (RSpec for Ruby, Vitest for TypeScript) following the conventions in your system prompt."
